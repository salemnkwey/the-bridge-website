**Card** — the base rounded surface for grouping content. Compose richer cards (course, testimonial) on top of it, or use directly.

```jsx
<Card tone="paper" hover>…</Card>
<Card tone="navy" radius="xl" pad="lg">…</Card>
```

- `tone="navy"` for inverse panels (final CTA, highlights); `cream` for subtle raised blocks.
